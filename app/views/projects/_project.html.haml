- @ind = @ind || 0
.project_wrapper{ :class=> ((@ind.even?) ? "even":"odd") }
  - @ind += 1
  = nav_link h(project.name), project
  .desc= textile_this project.description
  .bottom= "Created #{render_time project.created_at} ago"
